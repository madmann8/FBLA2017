import UIKit

struct YardSale {
  
  static let screen = UIScreen.main.bounds.size
  static let screenCenter = CGPoint(x: UIScreen.main.bounds.size.width / 2, y: UIScreen.main.bounds.size.height / 2)
  
  enum Control {
    case donateView
    case aboutFBLAView
//    case browseItemsView

  }
}
